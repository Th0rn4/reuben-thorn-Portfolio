// MainImage.jsx
import React from "react";
import "./MainImage.css"; // Import the CSS for the styles

const MainImage = () => {
  return <div className="main-image"></div>; // The div will have the background image applied in the CSS
};

export default MainImage;
